#include <bits/stdc++.h>
using namespace std;

int main(){
    freopen("DOIXUNG.INP", "r", stdin);
    freopen("DOIXUNG.OUT", "w", stdout);

    string s;
    cin >> s;

    int L, R;
    while(L >= 0 && R < S.size() && S[L] == S[R]){
        
    }
    return 0;
}
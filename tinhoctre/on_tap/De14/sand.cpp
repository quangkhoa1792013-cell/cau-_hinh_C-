#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    freopen("sand.INP", "r", stdin);
    freopen("sand.OUT", "w", stdout);

    int a, b, c, x, y;
    cin >> a >> b >> c >> x >> y;

    int tien = 0;

    while (a >= 0){
        if (a <= 0 || b <= 0 || c <= 0){
            tien
        }
    }

    return 0;
}
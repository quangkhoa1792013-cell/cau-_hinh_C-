#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int x, y, n;
    cin >> x >> y >> n;

    if (n > 500){
        int so_tien_them = n - 500;
        so_tien_them *= 
    }
    return 0;
}
#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int n, k;
    string s[n][k];
    cin >> n >> k;
    for (int i = 0; i < n; i++) {
        getline(cin, s);
    }
    return 0;
}